# extern/

CommonLibSSE is **not** vendored in this repository (size + submodule churn).

Clone powerofthree's CommonLibSSE dev branch before building:

```powershell
git clone --depth 1 --branch dev https://github.com/powerof3/CommonLibSSE.git extern/CommonLibSSE
```

See /docs/BUILD.md.
