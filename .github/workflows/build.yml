name: Build

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    # windows-2022 on purpose: the CMake presets target the VS2022 generator.
    runs-on: windows-2022
    timeout-minutes: 90
    defaults:
      run:
        working-directory: source-publish/SimpleOffenceSuppression

    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Resolve vcpkg
        shell: pwsh
        working-directory: .
        run: |
          $root = $env:VCPKG_ROOT
          if (-not $root) { $root = $env:VCPKG_INSTALLATION_ROOT }
          if (-not $root) { $root = "C:\vcpkg" }
          if (-not (Test-Path (Join-Path $root "scripts/buildsystems/vcpkg.cmake"))) {
            throw "Could not locate vcpkg under $root"
          }
          "VCPKG_ROOT=$root" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Fetch CommonLib submodules
        # The .gitmodules file lives inside source-publish/SimpleOffenceSuppression,
        # so actions/checkout cannot resolve it from the repo root; clone manually.
        shell: pwsh
        run: |
          git clone --depth 1 --branch dev https://github.com/powerof3/CommonLibSSE extern/CommonLibSSE
          # dev dropped include/REL (commonlib-shared migration, 2026-08-21); pin the
          # last pre-migration snapshot the plugin source + CMake probe target.
          git -C extern/CommonLibSSE fetch --depth 1 origin 86eb2b853101053390a81378b6263c2911603824
          git -C extern/CommonLibSSE checkout --quiet 86eb2b853101053390a81378b6263c2911603824
          git clone --depth 1 https://github.com/alandtse/CommonLibVR extern/CommonLibVR

      - name: Configure (AE preset)
        run: cmake --preset vs2022-windows-vcpkg-ae

      - name: Build
        # AE preset writes to buildae (CMakePresets.json), not build.
        run: cmake --build buildae --config Release --parallel 2

      - name: Verify DLL was produced
        shell: pwsh
        run: |
          $dll = Get-ChildItem -Recurse -Filter *.dll buildae | Select-Object -First 1
          if (-not $dll) { throw "Build produced no DLL." }
          Write-Host "Built $($dll.FullName)"
